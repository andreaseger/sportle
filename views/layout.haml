!!! 5
%html
  %head
    %title ScheduleDB
    %link(rel="stylesheet" href="/css/screen.css")  
    =# stylesheet('screen')
  %body
    - [:error, :notice].each do |name|
      - unless flash[name].nil? 
        %div{:id => "flash_#{name}"}=flash[name]
    %ul#nav
      %li
        %a(href="/s/new")new schedule
    %div#content
      = yield
      %p
        %a(href="/") show all
